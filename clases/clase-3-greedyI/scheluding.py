def scheduling(horarios):
    horarios_ordenador = ordenar_por_horario_fin(horarios) #O(nlogn)
    charlas = []
    # for horarios in horarios_ordenados: O(n)
    # if len(charlas) == 0 or not hay_interseccion(charlas[-1], horario): O(1) + O(1)
    #   charlas.append(horario)
    #
    
#O(nlogn)

